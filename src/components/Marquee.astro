---
import { Image } from 'astro:assets';
import kid from "../utils/images/compuer-kid.gif"
---
<section class="relative w-[100%]">
  <div class="marquee flex w-[100%] p-5 bg-orange-400 overflow-hidden uppercase">
    <h1> Fullstack * Backend * Frontend * API * MySql * NodeJs {" "}</h1>
  </div>
  
</section>

<style>
    .marquee {
        overflow: hidden;
        border-top: 1px solid #000;
        border-bottom: 1px solid #000;
        display: flex;
    }
    h1 {
        font-size: 3em;
        white-space: nowrap;  
    }
</style>
<script type="module">
function Marquee(selector, speed) {
  const parentSelector = document.querySelector(selector);
  const clone = parentSelector.innerHTML;
  const firstElement = parentSelector.children[0];
  let i = 0;
  parentSelector.insertAdjacentHTML('beforeend', clone);
  parentSelector.insertAdjacentHTML('beforeend', clone);

  setInterval(function () {
    firstElement.style.marginLeft = `-${i}px`;
    if (i > firstElement.clientWidth) {
      i = 0;
    }
    i = i + speed;
  }, 0);
}

//after window is completed load
//1 class selector for marquee
//2 marquee speed 0.2
window.addEventListener('load', Marquee('.marquee', 1))
</script>