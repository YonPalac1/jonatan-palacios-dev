---
import { fade } from "astro:transitions";
import { Image } from "astro:assets"
import duck from "../utils/images/duck.gif";

---
<div class="filter"></div>
<div class="bg"></div>
<header class="relative text-center uppercase pt-24 pb-24 text-white md:pt-54 lg:pt-64">
    <span class="tagline text-xs font-bold border border-white p-2">bienvenido</span>
    <h1 class="text-3xl mt-5 md:text-6xl">Hola! Soy Jonatan
        <span class="text-orange-400">Web developer</span>
    </h1>
    <p class="font-thin text-xs p-5 mb-10 md:text-sm"> Hago cosas para la web: e-commerce, landing pages y ese tipo de cosas </p>
    
    <div class="flex justify-center">
        <a class="font-bold flex justify-center w-64 flex text-orange-400" href="https://www.linkedin.com/in/palacios-yonatan/">Hablemos
            <svg class="icon icon-tabler icon-tabler-circle-arrow-right ml-3" width="24" height="24" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none"/><path d="M12 3a9 9 0 1 0 0 18a9 9 0 0 0 0 -18" /><path d="M16 12l-4 -4" /><path d="M16 12h-8" /><path d="M12 16l4 -4" /></svg>
        </a>
    </div>
    <Image class="absolute w-24" src={duck} alt="a duck walking" />
</header>
<style>
    .filter {
        width: 100%;
        height: 100%;
        position: fixed;
        top: 0;
        z-index: -1;
        background: radial-gradient(circle at var(--cursorXPos) var(--cursorYPos), rgba(255,0,0,0) 0%, black 30%, black 20%);
    }
    header {
        cursor: crosshair;
    }
    img {
        z-index: -2;
        right: 20rem;
        bottom: 0;
    }
    h1 {
        z-index: 5;
        display: block;
    }
    span.tagline {
        background: linear-gradient(90.21deg, rgba(255, 131, 14, 0.5) -5.91%, rgba(58, 61, 255, 0.5) 111.58%);
        font-family: 'Montserrat Alternates', sans-serif;
    }
    .bg {
        background-image: url('../utils/images/bg-dark.webp');
        background-position: center;
        background-repeat: no-repeat;
        background-size: cover;
        cursor: crosshair;
        position: fixed;
        height: 100%;
        width: 100%;
        z-index: -2;
    }
    a {
        animation: animate infinite 1s;
    }
    @keyframes animate {
        from {
           color: #ffbb00;}
        to {
            color: #fffb00;}
    }
</style>
<script type="module">
    const trackMouse = (e) => {
        document.documentElement.style.setProperty(
          "--cursorXPos",
          `${e.clientX}px`
        )
        document.documentElement.style.setProperty(
          "--cursorYPos",
          `${e.clientY}px`
        )
      }
    
    document.addEventListener("mousemove", trackMouse)
</script>